on:
  workflow_dispatch:
    inputs:
      message:
        description: "Test message"
        required: true
        default: "Hello"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print input
        run: echo "Message is: ${{ github.event.inputs.message }}"
